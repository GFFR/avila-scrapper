node_modules/
dist/
*.log
.DS_Store

# Secrets — never commit
.env
.env.local

# Postgres is the live store (Dokploy-managed, with its own backups). The
# canonical, vendor-portable backup committed here is data/json/.
